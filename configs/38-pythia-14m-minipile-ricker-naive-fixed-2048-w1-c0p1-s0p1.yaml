experiment_name: pythia_14m_minipile_ricker_naive_fixed_2048_w1_c0p1_s0p1
sweep:
  name: pressure_fixed_step_v1
  role: ricker_naive
  fixed_steps: 2048
  fixed_tokens: 134217728
  notes: Screening run for planning the full activation-pressure ablation.
model:
  provider: huggingface
  name: pythia-14m-random
  architecture: EleutherAI/pythia-14m-deduped
  revision: main
  initialization: random
data:
  name: JeanKaddour/minipile
  revision: main
  split: train
  text_column: text
  max_documents: null
evaluation:
  metric: training_loss
run:
  seed: 0
  max_examples: 1000000
tokenizer:
  name: EleutherAI/pythia-14m-deduped
  revision: main
preprocessing:
  output_dir: data/tokenized
  cache_id: 03-pythia-14m-minipile-random-full-10min
  block_size: 2048
  append_eos: true
  overwrite: false
training:
  max_steps: 2048
  max_wall_seconds: null
  warmup_steps: 100
  micro_batch_size: 4
  gradient_accumulation_steps: 8
  learning_rate: 3.0e-05
  precision: auto
  device: auto
  log_every: 50
validation:
  enabled: true
  split: validation
  max_documents: 500
  eval_every_steps: 250
  eval_batches: 8
  batch_size: 4
checkpoint:
  save_final: true
  save_optimizer: false
output:
  dir: results
activation_pressure:
  enabled: true
  method: ricker_naive
  sites:
  - mlp_hiddens
  weight: 1.0
  ricker_c: 0.1
  ricker_sigma: 0.1
  log_thresholds:
  - 0.0
  - 0.001
  - 0.003
  - 0.01
  - 0.03
